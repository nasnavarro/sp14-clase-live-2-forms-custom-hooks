import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Custom hooks, formularios y navegación sobre una API real.</p>
    </footer>
  );
}

export default Footer;
